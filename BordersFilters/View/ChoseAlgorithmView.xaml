<UserControl x:Class="BordersFilters.View.ChoseAlgorithmView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BordersFilters.View"
             xmlns:viewModel="clr-namespace:ViewModel;assembly=ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.DataContext>
        <viewModel:ChoseAlgorithmViewModel/>
    </UserControl.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <CheckBox Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center" IsChecked="{Binding RGBOperator, Mode=TwoWay}"/>
        <TextBox Grid.Column="1" PreviewTextInput="UIElement_OnPreviewTextInput" Text="{Binding ReapplyCount, Mode=TwoWay}"/>
        <ComboBox Grid.Column="2" ItemsSource="{Binding OperatorsList}" SelectedIndex="{Binding Operation, Mode=TwoWay}">
        </ComboBox>
    </Grid>
</UserControl>
